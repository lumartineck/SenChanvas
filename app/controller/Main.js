/**
 * @class SenChanvas.controller.Main
 * @extends Ext.app.Controller
 * The main controller of our app
 */
Ext.define('SenChanvas.controller.Main', {
    extend: 'Ext.app.Controller',

    config: {
        refs: {
            main: {
                selector: 'main'
            }
        }
    }
});